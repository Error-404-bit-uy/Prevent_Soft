<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bit.preventsoft">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PreventSoft">
        <activity
            android:name=".FormsListActivity"
            android:parentActivityName=".HomeActivity" />
        <activity android:name=".EditFormActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".NewFormActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".StockToolActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".EditToolActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".NewToolActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".ToolsMenuActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".PreventionsActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".RecomendationsActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".FormsMenuActivity"
            android:parentActivityName=".HomeActivity"/>
        <activity android:name=".HomeActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>